/*learning git
# to setup git => go to folder and type git init
# to see the status of project => type git status
# to add all the file to git repositary => git add . 
# to see a breif of log commits => git log --oneline
# to go back to last commit => git reset <file> ,git reset (this will revert to the last file)
*/

/* 
{github and git connect}
# add local repositary to online git {git remote add origin <repositary URL> }
# to push a project to a folder {git push -u origin master}
# go to a folder and hit { git clone <repositary url>  }


*/