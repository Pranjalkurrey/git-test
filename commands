
######step 1

/*learning git
# to setup git => go to folder and type git init
# to see the status of project => type git status
# to add all the file to git repositary => git add . 
# to see a breif of log commits => git log --oneline
# to go back to last commit => git reset <file> ,git reset (this will revert to the last file)
*/

######step 2
/* 
{github and git connect}
# add local repositary to online git {git remote add origin <repositary URL> }
# to push a project to a folder {git push -u origin master}
# go to a folder and hit { git clone <repositary url>  }


*/
#####step 3
{Node commands}

npm init => for setup of package.json
npm install lite-server --save-dev(save dev is for saving the nodu module in the package.json) => installs the node lite server
-> after installing node lite server go to script of package.json and edit
"scripts"{
    "start":"npm run lite",
    "test":no change,
    "lite":"lite-server"
}

######step 4
setup up the file .gitignore  
go to .gitignore 
type node_modules (this will exclude the node_module from being commited to git repositary)
